export const POSITION = 0, 
            VELOCITY = 1, 
            SPRITE = 2,
            COLLIDER = 3,
            INPUT = 4,
            TAG_PLAYER = 5,
            TAG_ENEMY = 6,
            TAG_PROJECTILE = 7,
            LIFETIME = 8,
            ANIMATOR = 9,
            FLIP_X = 10;

export const COMPONENTS = [
    POSITION,
    VELOCITY,
    SPRITE,
    COLLIDER,
    INPUT,
    TAG_PLAYER,
    TAG_ENEMY,
    TAG_PROJECTILE,
    LIFETIME,
    ANIMATOR,
    FLIP_X
]